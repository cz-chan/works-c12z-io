---
import styles from "@works-path/components/content-list.module.css";

export interface ContentListItem {
	title: string;
	href: string;
	subtitle?: string;
	meta?: string;
}

interface Props {
	items: ContentListItem[];
	title?: string;
	titleAs?: "h1" | "h2";
}

const { items, title, titleAs: Title = "h2" } = Astro.props;
---

<section>
	{
		title && (
			<Title
				class={styles.title}
				lang="en"
			>
				{title}
			</Title>
		)
	}

	<ul
		class={styles.list}
		role="list"
	>
		{
			items.map((item) => (
				<a
					href={item.href}
					class={styles.workList}
				>
					<li class={styles.entry}>
						<span class={styles.entryText}>
							<p class={styles.company}>{item.title}</p>
							{item.subtitle && (
								<span class={styles.entrySubtitle}>{item.subtitle}</span>
							)}
						</span>
						{item.meta && <span class={styles.entryMeta}>{item.meta}</span>}
					</li>
				</a>
			))
		}
	</ul>
</section>
