---
import styles from "./main.module.css";

interface Props {
	backHref?: string;
}

const { backHref } = Astro.props;
---

<main class={styles.content}>
	{
		backHref && (
			<a
				class={styles.goBack}
				href={backHref}
			>
				go back
			</a>
		)
	}
	<slot />
</main>
